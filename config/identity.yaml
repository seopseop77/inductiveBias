# ===== Identity baseline (report Section 4.2) =====
# Token mixer replaced by the identity function, i.e. no token mixing at all.
# Isolates how much of the accuracy comes from the channel mixer alone.
model: identity
